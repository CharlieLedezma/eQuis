## snake_physical_model
Modeling of a snkake like a robot 

## Install the package locally

# Installation
$ pip install -e .

## Checking installation
$ pip show snake_physical_model

##
init:
    pip install -r requirements.txt
    
    
   python3 -m pip install --upgrade build